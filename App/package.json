{
  "name": "app",
  "version": "0.0.0",
  "engines": {
    "node": "18.x",
    "npm": "8.x"
  },
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "npm run-script ng build --configuration=production",
    "watch": "ng build --watch --configuration development",
    "test": "ng test"
  },
  "private": true
}
